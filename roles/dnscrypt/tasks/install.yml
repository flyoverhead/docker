---

- name: install | dnscrypt service folder
  become: true
  ansible.builtin.file:
    path: '{{ dnscrypt_docker_config.path }}'
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: install | dnscrypt docker-compose file
  become: true
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: '{{ dnscrypt_docker_config.path }}/docker-compose.yml'
    owner: root
    group: root
    mode: '0644'
    lstrip_blocks: true
  notify:
    - dnscrypt | start container
