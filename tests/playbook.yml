---

- name: test | deploy services
  hosts: all

  roles:

    - role: node-exporter
      when: '"node_exporter" in group_names'

    - role: prometheus
      when: '"node_exporter" in group_names'

    - role: alertmanager
      when: '"node_exporter" in group_names'

    - role: grafana
      when: '"node_exporter" in group_names'

    - role: dnscrypt
      when: '"node_exporter" in group_names'

    - role: pihole
      when: '"node_exporter" in group_names'

    - role: xray
      when: '"node_exporter" in group_names'

    - role: wireguard
      when: '"node_exporter" in group_names'

    - role: torrserver
      when: '"node_exporter" in group_names'
